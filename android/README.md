# qWDTT

Тоннель WireGuard с телефона на ваш VPS через **TURN-релей VK**. Для сети снаружи это выглядит как обычный WebRTC-звонок, а не как VPN.

**Пакет:** `net.qwdtt.client.jewbsv` · **Версия:** 1.4.40

Полностью совместим с сервером и протоколом **WDTT** ([amurcanov/proxy-turn-vk-android](https://github.com/amurcanov/proxy-turn-vk-android)): `wdtt-server`, ссылки `wdtt://`, WRAP/RTP и VK-авторизация работают взаимозаменяемо — можно поднять VPS оригинальным деплоем и подключиться qWDTT.

---

## ⚠️ Дисклеймер / Disclaimer

> Приложение является техническим инструментом для защищённого туннелирования трафика через ваш собственный TURN-сервер. Проект qWDTT носит исключительно витринно-ознакомительный характер на примере уже имеющейся структуры в целях исследования сетевых протоколов.
> Я не призываю использовать qWDTT для обхода блокировок или нарушения правил каких-либо платформ, а также не несу ответственности за финальные сценарии использования утилиты пользователями в реальной жизни или сети ИНТЕРНЕТ. Любые специфические технические особенности приложения — не более чем архитектурное совпадение, созданное без какого-либо умысла.
> Проект является полностью некоммерческим, не содержит платных функций, скрытых подписок или коммерческой выгоды.

---

## 🤔 Что это

Утилита туннелирует трафик с телефона на ваш сервер. Отличие от обычного VPN — транспорт:

- клиент получает временные **TURN**-креды из звонка VK;
- поднимает **DTLS** через TURN-релей VK;
- пакеты заворачиваются в **RTP** и шифруются **WRAP** (ChaCha20-Poly1305);
- на телефоне — системный **WireGuard** (`VpnService`), на сервере — `wdtt-server`.

```
Телефон                       VK TURN relay                VPS
┌──────────────┐   RTP+WRAP    ┌──────────┐  DTLS+WRAP   ┌──────────────┐
│ WireGuard    │ ────────────► │  relay   │ ───────────► │ wdtt-server  │
│ Go-клиент    │               │   (VK)   │              │  WireGuard   │
└──────────────┘               └──────────┘              └──────────────┘
```

## ✨ Возможности

- **Профили** — несколько конфигов, папки, перетаскивание, быстрый выбор на «Туннеле».
- **Подписки** — раздача профилей по HTTPS-JSON, автообновление, трафик на карточке.
- **Импорт** — `wdtt://`, расширенные `qwdtt://`, QR, пакетный JSON, файл `.qwdtt`.
- **VK** — анонимный вход или через аккаунт (WebView, перехват `turn_server`).
- **Туннель** — логи, статистика, пинг до сервера, смена потоков, DNS, обход IP/доменов и исключения приложений.
- **Деплой сервера** — установка `wdtt-server` на VPS по SSH прямо с телефона.
- **Удобства** — виджет, плитка Quick Settings, watchdog при смене сети, статус пароля в карточке.

> Чего нет в оригинальном WDTT: профили, подписки, глобальные VK-хеши, пинг, вход через аккаунт VK, виджет и плитка. Протокол при этом общий.

## 🚀 Быстрый старт

1. Установите APK из [релизов](https://github.com/jewbsv/proxy-turn-vk-android/releases).
2. Настройте сервер: вкладка **Деплой** или уже готовый VPS с `wdtt-server`.
3. Запустите звонок VK: ссылка `vk.com/call/join/…` или только хеш после `/join/`.
4. Импортируйте профиль: `wdtt://…`, `qwdtt://…`, QR или заполните вручную.
5. Нажмите **Подключить** и разрешите VPN.

> ⚠️ Завершайте звонок **«Просто завершить»**, а не «Завершить для всех» — иначе хеш умрёт.
> До **4 хешей** в профиле — нагрузка распределяется между звонками.

## 🔗 Форматы ссылок

**WDTT** — официальный, поддерживается полностью (QR, буфер, бот сервера):

```text
wdtt://<IP>:<dtls_port>:<wg_port>:<local_port>:<пароль>:<vk_hash>
```

| Поле | По умолчанию | Описание |
|------|--------------|----------|
| `dtls_port` | `56000` | UDP-порт DTLS на VPS |
| `wg_port` | `56001` | внутренний WireGuard-порт сервера |
| `local_port` | `9000` | локальный UDP на Android |
| `пароль` | — | пароль подключения (WRAP + авторизация) |
| `vk_hash` | — | хеш звонка; `:` внутри хеша склеивается |

**qWDTT** — расширенный, удобнее для профилей (имя, несколько хешей, потоки):

```text
qwdtt://config?name=Дом&peer=1.2.3.4:56000&hashes=хеш1,хеш2&workers=18&port=9000&pass=секрет
```

**JSON / `.qwdtt`** — профили описываются простым JSON (`name`, `peer`, `hashes`, `workers`, `port`, `password`; альтернативы: `vkHashes`, `workersPerHash`, `listenPort`, `pass`). Файл `.qwdtt` — URI или JSON внутри.

**Подписки** — один HTTPS-JSON раздаёт список профилей; приложение обновляет папку по кнопке или периодически.

```json
{
  "subscriptionName": "Мой сервер",
  "description": "Подписка · до 24.08.2026",
  "trafficUsedMb": 76.8,
  "trafficLimitMb": 102912,
  "profiles": [
    { "name": "Сервер 1", "peer": "203.0.113.10:56000", "hashes": "vk_hash_1", "workers": 18, "port": 9000, "password": "секрет" }
  ]
}
```

## 🖥 Сервер

`server.go` → бинарь `wdtt-server`: DTLS, WRAP/RTP, выдача WG-конфига, `/etc/wdtt/passwords.json`, Telegram-бот.

| Параметр | Значение |
|----------|----------|
| `56000/udp` | DTLS-приём клиентов |
| `56001/udp` | WireGuard внутри сервера |
| `9000/udp` | локальный порт на Android |
| `10.66.0.0/16` | подсеть клиентов (типичный адрес `10.66.66.x`) |

Пароли: главный + до 10 пользовательских (срок, привязка к устройству). Обновление без рестарта: правка JSON → `kill -HUP $(pidof wdtt-server)`.

### Сохранение базы клиентов

При деплое через приложение база `passwords.json` **автоматически сохраняется** и восстанавливается. Все пользовательские имена клиентов (созданные через Telegram-бота) остаются после переустановки сервера. Добавлено в версии **v1.4.34**.

## 🤖 Telegram-бот

Сервер `wdtt-server` включает встроенного Telegram-бота для удобного управления клиентами.

### Возможности бота

- **Управление клиентами** — создание, просмотр и настройка доступов.
- **Пользовательские имена** — при создании клиента вы даёте ему имя (например, `Evgen`), которое используется в конфигах и списках.
- **Изменение лимита устройств** — настройка максимального числа одновременных подключений для каждого клиента.
- **Изменение имени клиента** — кнопка в карточке клиента для переименования уже созданного доступа.
- **Удобное меню** — постоянные кнопки внизу экрана:
  - ➕ **Создать клиента** — пошаговый мастер создания нового доступа.
  - 👥 **Управление клиентами** — просмотр списка всех клиентов с их именами.

### Команды

- `/start`, `/help` — приветствие и основное меню.
- `/new` — создать нового клиента (запрашивает имя, срок, лимит устройств, порты, VK-хеш).
- `/list` — показать список всех клиентов с кнопками управления.
- `/menu` — вызвать главное меню.

### Создание клиента (пошагово)

1. **Имя клиента** — бот запрашивает имя (например, `Evgen`).
2. **Срок и лимит** — количество дней и максимум устройств.
3. **Порты** — DTLS-порт и WireGuard-порт (или используются значения по умолчанию).
4. **VK-хеш** — хеш звонка VK для TURN-релея.
5. **Генерация конфига** — бот создаёт профиль с пользовательским именем и отправляет ссылку `wdtt://`.

В списке клиентов (`/list`) каждый отображается с присвоенным именем и кнопкой 🔍 для просмотра деталей.

> **Примечание:** улучшения интерфейса бота были добавлены в версиях **v1.4.31–v1.4.32**; кнопка переименования — в версии **v1.4.38**.

## 📊 Имена клиентов в StatusOpenVPN

[StatusOpenVPN](https://github.com/TheMurmabis/StatusOpenVPN) — это веб-панель для мониторинга клиентов WireGuard/OpenVPN: статусы, трафик, IP-адреса и читаемые имена пиров. Она читает конфиги из `/etc/wireguard/` и берёт имена из комментариев `# Client = ...`.

Для интерфейса `wdtt0` из qWDTT/WDTT такого конфига нет, поэтому в StatusOpenVPN имена отображаются как `N/A`.

**Важно:** скрипты ниже — это патч для уже установленного StatusOpenVPN, а не его установка. Сам StatusOpenVPN должен быть развёрнут заранее.

В папке [`Wdtt0/`](Wdtt0/) лежат скрипты, которые:

- генерируют `/etc/wireguard/wdtt0.conf` из имён клиентов Telegram-бота (`/etc/wdtt/passwords.json`);
- пропатчивают StatusOpenVPN так, чтобы он читал этот конфиг;
- обновляют имена по cron.

Быстрая установка патча на VPS (как root):

```bash
bash -c "$(curl -sL https://raw.githubusercontent.com/jewbsv/proxy-turn-vk-android/master/Wdtt0/setup.sh)"
```

Подробности и удаление — в [`Wdtt0/README.md`](Wdtt0/README.md).

## 🛠 Сборка

```bash
export ANDROID_NDK_HOME=/path/to/android-ndk
./scripts/build-native-libs.sh
./gradlew :app:assembleDebug
```

Одна ABI: `./scripts/build-go-lib.sh arm64-v8a`. Имена выходных APK: `qWDTT-<abi>.apk` (например `qWDTT-arm64-v8a.apk`, `qWDTT-x86_64.apk`, `qWDTT-universal.apk`).

## 📁 Структура репозитория

```text
app/        Android-клиент (Kotlin + Compose, VPN, WebView)
go_client/  Нативный клиент → libclient.so
server.go   Серверная часть (совместим с WDTT)
scripts/    Сборка Go под Android ABI
```

---

## 📄 Лицензия

[GNU GPL v3](LICENSE)

## 🌱 Происхождение

Форк [proxy-turn-vk-android](https://github.com/amurcanov/proxy-turn-vk-android) (автор **amurcanov**). Протокол и сервер общие; клиентская часть переработана под профили, импорт и удобство повседневного использования. Разработка форка — [jewbsv/proxy-turn-vk-android](https://github.com/jewbsv/proxy-turn-vk-android).
