name: VKTurnProxy
options:
  bundleIdPrefix: com.vkturnproxy
  deploymentTarget:
    iOS: "15.0"
  xcodeVersion: "16.2"

settings:
  base:
    DEVELOPMENT_TEAM: CDMQ33VFQC
    CODE_SIGN_STYLE: Automatic

targets:
  VKTurnProxy:
    type: application
    platform: iOS
    sources:
      - path: VKTurnProxy
    # Switched from GENERATE_INFOPLIST_FILE: YES to an explicit
    # info.properties block in xcodegen. Reason: CFBundleURLTypes (an
    # array-of-dicts for the vkturnproxy:// 1-Click import scheme) has
    # no INFOPLIST_KEY_* equivalent — Apple's flat KEY_VALUE form only
    # covers scalars. The translations from the old INFOPLIST_KEY_*
    # entries to native plist values are 1:1 below; CFBundleVersion /
    # CFBundleShortVersionString are interpolated explicitly via
    # $(...) so they still track CURRENT_PROJECT_VERSION / MARKETING_VERSION
    # in build settings below.
    info:
      path: VKTurnProxy/Info.plist
      properties:
        CFBundleDisplayName: "VK Turn Proxy"
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        # Declare that we don't use non-exempt encryption — only standard
        # crypto exposed through Apple system APIs (CommonCrypto,
        # Security.framework, Network.framework DTLS/TLS), all exempt
        # under U.S. export Category 5 Part 2. Without this, App Store
        # Connect blocks every TestFlight build with "Encryption export
        # compliance documentation needed" + a France-specific prompt.
        # Was lost in an earlier xcodegen regenerate; pinned here now.
        ITSAppUsesNonExemptEncryption: false
        # Required before ActivityKit will start a Live Activity at all
        # (GitHub issue #64). Without it Activity.request throws and the
        # island never appears — with no other symptom.
        NSSupportsLiveActivities: true
        # UIApplicationSceneManifest_Generation: YES used to produce a
        # dict with UIApplicationSupportsMultipleScenes=false; same
        # shape written explicitly below.
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: false
        UIApplicationSupportsIndirectInputEvents: true
        # UILaunchScreen_Generation: YES used to produce an empty dict
        # (iOS shows a blank launch screen with the default tint); empty
        # mapping below has the same effect.
        UILaunchScreen: {}
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        # 1-Click Connection Link import. Tapping a
        # vkturnproxy://import?data=<base64> URL anywhere on iOS
        # launches us and delivers the URL via .onOpenURL at the
        # WindowGroup; ConnectionLinkImporter is what acts on it (see
        # ConnectionLinkImport.swift). The base64 payload is a JSON ConnectionLink
        # (see AppConfig.swift + BackupManager.parseConnectionLink).
        # Generator script: quick_link.py at repo root.
        CFBundleURLTypes:
          - CFBundleTypeRole: Editor
            CFBundleURLName: com.vkturnproxy.app
            CFBundleURLSchemes:
              - vkturnproxy
              # amurcanov proxy-turn-vk-android compat. His server hands out
              # wdtt://<ip>:<dtls>:<wg>:<peer>:<password>:<hash[,hash…]> links.
              # His own Android app does NOT register the scheme (paste-only),
              # so we become the sole handler — no collision. Parsed by
              # BackupManager.parseWdttLink into SRTP-WRAP-A mode.
              - wdtt
              # samosvalishe free-turn-proxy compat. freeturn://<base64url(json)>
              # links (internal/uri/uri.go). Parsed by BackupManager.parseFreeturnLink
              # into SRTP-WRAP-S mode; carries no VK link / WG keys (user fills those).
              - freeturn
              # amurcanov csqtt compat (stage 5). csqtt://connect?v=2&host=&peer=
              # &password=[&hashes=] and the legacy csqtt://<password>@<host>:<port>.
              # Parsed by BackupManager.parseCsqttLink into csqtt mode.
              - csqtt
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.vkturnproxy.app
        CODE_SIGN_ENTITLEMENTS: VKTurnProxy/VKTurnProxy.entitlements
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: 381
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        # Pre-bootstrap captcha probe in connect() calls Go-side
        # wgProbeVKCreds directly from main-app process (the only context
        # where we can show a WebView while still having network — see
        # TunnelManager.swift). That requires the same xcframework +
        # bridging header that the extension target uses.
        SWIFT_OBJC_BRIDGING_HEADER: VKTurnProxy/BridgeHeader.h
        OTHER_LDFLAGS:
          - "-lresolv"
        FRAMEWORK_SEARCH_PATHS:
          - "$(inherited)"
          - "$(PROJECT_DIR)/../WireGuardBridge/build"
    dependencies:
      - target: PacketTunnel
        embed: true
      # Live Activity / widget host (GitHub issue #64). Embedded into PlugIns
      # like the tunnel extension. Nothing in the app links against it — the
      # only shared code is VPNActivityAttributes.swift, compiled into both.
      - target: VKTurnProxyWidget
        embed: true
      - sdk: NetworkExtension.framework
      # App Intents back the Live Activity's buttons (issue #64 stage 2); their
      # perform() runs in THIS process.
      - sdk: AppIntents.framework
      # Main-app links against the xcframework (calls wgProbeVKCreds for
      # the pre-bootstrap captcha probe) but must NOT embed it: the
      # underlying file is a static archive (libwg-turn-ios-arm64.a), and
      # App Store Connect rejects archives in Frameworks/ with
      # ITMS-90432/ITMS-90171. The PacketTunnel extension already embeds
      # the xcframework into its own bundle for runtime; the main app
      # just links the symbols at build time.
      - framework: ../WireGuardBridge/build/WireGuardTURN.xcframework
        embed: false

  PacketTunnel:
    type: app-extension
    platform: iOS
    sources:
      - path: PacketTunnel
      # SharedLogger.swift lives under VKTurnProxy/ but is compiled into
      # both targets — main-app and extension write to the same App Group
      # log file via this helper.
      - path: VKTurnProxy/SharedLogger.swift
      # OSLogReader.swift is the in-process os_log fallback. Compiled
      # into the extension so handleAppMessage("get_logs") can read its
      # own ring buffer and return entries to the main app — used as a
      # fallback when the App Group container is unavailable and
      # SharedLogger silently no-ops.
      - path: VKTurnProxy/OSLogReader.swift
      # VKCookieStore.swift — Keychain-backed VKAuth cookie store, compiled into
      # the extension too so it can read the logged-in cookie at startTunnel
      # (shared keychain-access-group, same App Group as the rest).
      - path: VKTurnProxy/VKCookieStore.swift
      # DirectRouteSync.swift — the desired/applied/in-flight machine behind
      # DIRECT mode. It lives under VKTurnProxy/ rather than PacketTunnel/ for
      # ONE reason: the extension has no test target, and tools/swiftcheck
      # compiles the REAL source standalone. A copy under PacketTunnel/ would be
      # a second file to keep in step with the one the harness checks.
      - path: VKTurnProxy/DirectRouteSync.swift
      # AppEntitlements.swift — reads this process's own code signature so the
      # App-Group failure can name the team that signed the build. Compiled into
      # the extension too: it has its OWN signature, and app/extension can
      # disagree, which is itself a finding worth logging.
      - path: VKTurnProxy/AppEntitlements.swift
      # TunnelMTU.swift — the MTU bounds. The extension clamps again at the
      # sink: providerConfiguration carries whatever ended up in UserDefaults,
      # and an imported backup is not a trusted source.
      - path: VKTurnProxy/TunnelMTU.swift
    info:
      path: PacketTunnel/Info.plist
      properties:
        CFBundleDisplayName: "VK Turn Proxy Tunnel"
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        NSExtension:
          NSExtensionPointIdentifier: com.apple.networkextension.packet-tunnel
          NSExtensionPrincipalClass: $(PRODUCT_MODULE_NAME).PacketTunnelProvider
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.vkturnproxy.app.tunnel
        CODE_SIGN_ENTITLEMENTS: PacketTunnel/PacketTunnel.entitlements
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: 381
        SKIP_INSTALL: YES
        SWIFT_OBJC_BRIDGING_HEADER: PacketTunnel/BridgeHeader.h
        OTHER_LDFLAGS:
          - "-lresolv"
        FRAMEWORK_SEARCH_PATHS:
          - "$(inherited)"
          - "$(PROJECT_DIR)/../WireGuardBridge/build"
    dependencies:
      - framework: ../WireGuardBridge/build/WireGuardTURN.xcframework
      - sdk: NetworkExtension.framework

  # ─── Live Activity / widget extension (GitHub issue #64) ──────────────────
  # Stage 1 is display-only: connection state + active server name on the Lock
  # Screen and in the Dynamic Island. Deliberately NOT part of the app target's
  # iOS 15.0 floor — ActivityKit with staleDate is 16.2+, so this target overrides the
  # deployment target and the app keeps supporting older devices without it.
  # No App Group and no entitlements file: the system delivers ContentState to
  # this process, so nothing here reads shared storage. That is also why this
  # part survives on third-party re-signed builds, unlike anything App-Group
  # backed (see reference: re-signed IPA breaks the App Group).
  VKTurnProxyWidget:
    type: app-extension
    platform: iOS
    deploymentTarget: "16.2"
    sources:
      - path: VKTurnProxyWidget
      # The Live Activity contract, shared with the app the same way
      # SharedLogger.swift is shared with the tunnel extension.
      - path: VKTurnProxy/VPNActivityAttributes.swift
      # Stage 2's buttons. The intent TYPES must exist in this process for
      # Button(intent:) to reference them; their perform() bodies only run in
      # the app, which is why the file routes through a handler instead of
      # touching TunnelManager/ServerStore (unavailable here by design).
      - path: VKTurnProxy/LiveActivityIntents.swift
    info:
      path: VKTurnProxyWidget/Info.plist
      properties:
        CFBundleDisplayName: "VK Turn Proxy Widget"
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.vkturnproxy.app.widget
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: 381
        SKIP_INSTALL: YES
    dependencies:
      - sdk: WidgetKit.framework
      - sdk: SwiftUI.framework
      - sdk: AppIntents.framework
